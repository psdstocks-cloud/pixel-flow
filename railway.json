{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "find apps/api/dist -name 'index.js' -type f && INDEX_PATH=$(find apps/api/dist -name 'index.js' -type f | head -1) && echo \"Found index.js at: $INDEX_PATH\" && node \"$INDEX_PATH\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}