{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "echo '=== Listing /app directory ===' && ls -la /app && echo '=== Listing apps/api ===' && ls -la apps/api && echo '=== Listing apps/api/dist ===' && ls -la apps/api/dist && echo '=== Starting server ===' && node apps/api/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}