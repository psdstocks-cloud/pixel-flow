{
  "name": "pixel-flow",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:db": "npm run build --workspace=@pixel-flow/database",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "start:api": "npm run build --workspace=api && npm start --workspace=api",
    "format": "prettier --write .",
    "postinstall": "npm --prefix packages/database run generate && npm --prefix packages/database run build"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.10",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "turbo": "^2.0.6",
    "typescript": "^5.4.5",
    "tailwindcss": "^3.4.17",
  "postcss": "^8.4.0",
  "autoprefixer": "^10.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "mixpanel-browser": "^2.71.0",
    "next": "^14.2.25",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
